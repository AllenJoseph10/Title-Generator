# Dependencies
node_modules

# Next.js build output
.next
.vercel
next-env.d.ts
*.tsbuildinfo

# Environment & secrets
.env
.env.local
.env.*.local
vercel recovery-codes.txt
*recovery-codes*
*.pem
*.key
# Briefs/specs handed over as documents may embed live credentials
*.docx

# Datasets — never commit raw creator content
Will Wade Dataset/
*Dataset/
*/dataset/
*.mp4
*.mov

# Instagram scrape working cache (manifests, debug samples) — not the final dataset
datasets/raw/

# Editor / OS
.DS_Store
.claude/
.vscode/
.idea/
*.log
